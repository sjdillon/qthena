_CONFIG = {
    # moved these to the event- makes no sense in a config once you think of this as a standalone moodule
    # "db": "default",
    # "bucket_format": "s3-{prefix1}-{prefix2}-{env}-{region}-data",
    # "s3_query_results_path": "awsathenadata/product/queryresults",
    "sleep_seconds": 5
}
